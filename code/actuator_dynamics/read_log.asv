%% Experiment comments:


fileID = fopen('putty.log');
A = fread(fileID);

fclose(fileID);

%%

B = zeros(floor(length(A)/2),1);

for n=1:2:length(A)
    B((n+1)/2) = swapbytes(typecast(uint8(A(n:(n+1))), 'uint16'));
end
